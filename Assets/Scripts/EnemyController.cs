using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//GetComponent<AIPath>().target = 
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
